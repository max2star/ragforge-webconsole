@@ -370,7 +370,7 @@
       <Spin spinning={loading}>
         <section className={styles.modelContainer}>
           <SettingTitle
-            title={t('modelProviders')}
+            title={t('model')}
             description={`${t(
               'modelDescription',
             )} （先"添加模型" -> 再"设置默认模型"）`}
